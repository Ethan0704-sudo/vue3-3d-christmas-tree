````markdown
# 🎄 雪落星辰 (Snowfall Starlight) | 你的私人 3D 互动圣诞树

> **“传说这面魔镜采集了十二月的星光，能将美好的回忆凝结成永恒的水晶。”** ❄️✨

**雪落星辰** 是一个基于 Web 的沉浸式 3D 圣诞体验项目。它不仅仅是一棵树，更是一个结合了 **计算机视觉 (Computer Vision)** 与 **高性能 WebGL 渲染** 的互动艺术品。通过 AI 手势识别，你可以掌控风雪、重组回忆，并在奢华的数字空间中许下愿望。

## ✨ 核心亮点 (Key Features)

### 🎨 极致视觉体验

- **5000+ 粒子系统**：使用 `THREE.InstancedMesh` 技术构建的高保真松针，在保证 60FPS 流畅度的同时提供震撼的体积感。

- **电影级光效**：集成 HDR 环境贴图、UnrealBloom 辉光特效以及动态的冷暖光照系统。

- **魔法调色盘**：内置 **经典红绿 (Classic)**、**极地银蓝 (Frozen)**、**午夜黑金 (Midnight)** 三套高定主题，一键无缝切换氛围。

### ✋ AI 手势交互 (MediaPipe Magic)

告别鼠标，用魔法掌控一切。基于 Google MediaPipe 的手部追踪技术：

- 👋 **挥手 (Palm Move)**：实时控制场景视角的旋转。

- ✊ **握拳 (Fist)**：召唤粒子聚拢，形成经典的圣诞树形态 (TREE Mode)。

- ✨ **张开 (Open Hand)**：释放粒子，化作环绕式的全景画廊 (SCATTER Mode)。

- 👌 **捏合 (Pinch)**：通过虚拟光标精准选中悬浮的照片，镜头平滑推轨聚焦 (FOCUS Mode)。

### 📸 沉浸式回忆画廊

- **拍立得风格**：上传的照片会自动生成带有物理质感的拍立得边框。

- **螺旋排列**：在树形模式下，照片沿树体螺旋上升；在散开模式下，化作悬浮的星环。

- **智能透视**：基于距离的动态缩放，让每一张回忆都触手可及。

## 🛠️ 技术栈 (Tech Stack)

- **Core**: [Vue 3](https://vuejs.org/) (Composition API, Script Setup)

- **Build Tool**: [Vite](https://vitejs.dev/)

- **3D Engine**: [Three.js](https://threejs.org/)

- **Computer Vision**: [@mediapipe/tasks-vision](https://www.npmjs.com/package/@mediapipe/tasks-vision)

- **Styling**: CSS3 (Glassmorphism / 磨砂玻璃 UI)

## 🚀 快速开始 (Quick Start)

### 环境要求

- Node.js > 16.x

- 支持 WebGL 的现代浏览器

- 摄像头（用于手势体验，可选）

### 安装与运行

1. **克隆项目** (欢迎 Star! ⭐️)

   ```bash
   git clone [https://github.com/your-username/snowfall-starlight.git](https://github.com/your-username/snowfall-starlight.git)
   cd snowfall-starlight
   ```
````

2. **安装依赖**

```bash
npm install

```

3. **启动开发服务器**

```bash
npm run dev

```

4. 打开浏览器访问本地地址（通常是 `http://localhost:5173`），开启你的圣诞魔法之旅。

### 构建部署

```bash
npm run build

```

## 📖 操作指南 (User Guide)

| **动作**     | **触发方式**                        | **效果**                     |
| ------------ | ----------------------------------- | ---------------------------- |
| **开启体验** | 点击首页“开启圣诞魔法”              | 进入 3D 场景并播放背景音乐   |
| **旋转视角** | 鼠标拖拽 / 手掌左右移动             | 360 度欣赏你的作品           |
| **切换形态** | 键盘 `H` (显隐 UI) / 手势握拳与张开 | 在圣诞树与星空画廊间切换     |
| **上传照片** | 点击底部 `📷` 图标                  | 支持多选，最多 50 张         |
| **更换背景** | 点击底部 `🌄` 图标                  | 上传自定义背景图，增强沉浸感 |
| **手势开关** | 点击底部 `✨` 图标                  | 开启摄像头进行无接触交互     |
| **切换主题** | 点击底部 `🎨` 图标                  | 在三种预设配色中切换         |

## 📂 项目结构 (Structure)

```
src/
├── components/        # UI 子组件
│   ├── AppTitle.vue       # 顶部标题栏
│   ├── BlessingModal.vue  # 祝福语弹窗
│   ├── ControlBar.vue     # 底部功能控制栏
│   ├── Loader.vue         # 加载画面与入口
│   ├── MusicPlayer.vue    # 悬浮音乐播放器
│   ├── StatusPanel.vue    # 左上角手势状态面板
│   └── ThemePalette.vue   # 主题切换浮层
├── composables/       # 核心逻辑封装 (Composition API)
│   ├── useChristmasScene.ts  # Three.js 场景、粒子、光照与渲染循环
│   └── useGestureControl.ts  # MediaPipe 手势识别与射线检测交互
├── types/             # TypeScript 类型定义
│   └── index.ts       # Theme, FoliageData, SceneMode 等接口定义
├── utils/             # 工具类
│   └── TextureFactory.ts # Canvas 动态纹理生成 (文字、礼物贴图等)
├── views/             # 页面级组件
│   └── ChristmasTree.vue # 主入口视图 (负责组合 UI 与 Logic)
├── App.vue            # 根组件
└── main.js            # 入口文件

```

## 🤝 贡献 (Contributing)

我们非常欢迎社区的贡献！无论是优化 3D 性能、添加新的节日主题，还是改进手势识别的准确度。

1. Fork 本项目 🍴
2. 创建你的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交你的改动 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启一个 Pull Request 🚀

## 📜 许可证 (License)

本项目采用 [MIT License](https://www.google.com/search?q=LICENSE) 开源。你可以自由地使用、修改和分发，以此来传递节日的快乐。

<div align="center">
<p>如果你喜欢这个项目，请点亮右上角的 ⭐️ Star！</p>
<p>Made with ❤️ by [你的名字]</p>
<p>🎄 Merry Christmas & Happy New Year 🎄</p>
</div>

```

```
